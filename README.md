Open a terminal and use the gcc compiler:
gcc server.c -o server 
gcc client.c -o client 
Run the Server:      ./server
Run the Client:      ./client


3 Save the code into a file named socket_options.c. 
Open terminal, navigate to the directory containing the file, and compile it
gcc socket_options.c -o socket_options
